nav.projects-navigation
  .previous
    - prev_project = project.previous_article
    - if prev_project
      = link_to "/briansutter/#{prev_project.path}", relative: false, rel: 'prev' do
        | Previous project
        br
        strong= prev_project.title
    - else
      | &nbsp;

  .next
    - next_project = project.next_article
    - if next_project
      = link_to "/briansutter/#{next_project.path}", relative: false, rel: 'next' do
        | Next project
        br
        strong= next_project.title
    - else
      | &nbsp;
